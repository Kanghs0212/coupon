# 대규모 트래픽 처리를 위한 선착순 쿠폰 발급 시스템

짧은 시간에 트래픽이 집중되는 '선착순 이벤트(한정판 쿠폰 발급)' 환경을 모티브로 만든 백엔드 시스템입니다.

목표는 단순했습니다. 대규모 동시 요청에서 생기는 데이터 정합성 문제(Race Condition)를 해결하고, 시스템의 성능 한계를 부하 테스트로 수치화한 뒤, 그 수치를 근거로 아키텍처를 단계적으로 개선해 나가는 것. 이 README는 그 개선 과정을 실패까지 포함해 기록한 문서입니다.

## 1. 기술 스택

- **Language:** Java 17
- **Framework:** Spring Boot 3.5.14
- **Build:** Gradle
- **Database:** H2 (단위 테스트), PostgreSQL 15 (부하 테스트), Redis (분산 락 / 캐싱)
- **ORM:** Spring Data JPA, Hibernate
- **Test & Performance:** JUnit 5, AssertJ, Apache JMeter
- **Infra:** Docker & Docker Compose

## 2. 도메인 설계

핵심인 `Coupon` 도메인에 비즈니스 로직을 응집시켜 객체지향적으로 설계했습니다.

- **Coupon Entity:** 식별자(ID), 이름(Name), 남은 수량(Quantity)을 관리.
- **로직 캡슐화:** 수량 차감은 `decrease()` 메서드 안에서만 일어나고, 0 이하가 되면 `IllegalArgumentException`을 던집니다. 외부에서 수량을 임의로 조작하지 못하도록 막은 것입니다.

본격적인 동시성 테스트에 앞서, H2 인메모리 DB와 `@SpringBootTest`로 스프링 컨텍스트 로드와 기본 CRUD가 정상 동작하는지부터 검증하고 시작했습니다.

## 3. 트러블슈팅: 성능 최적화 과정

### Phase 1. 동시성 문제 재현 (Race Condition)

먼저 문제를 눈으로 보기 위해, 100명이 동시에 쿠폰을 요청하는 상황을 만들었습니다. `ExecutorService`로 32개 스레드 풀을 만들고 `CountDownLatch`로 100개 요청이 동시에 `decrease()`에 진입하도록 강제했습니다.

쿠폰 100개가 모두 차감되어 0이 되어야 했지만, 결과는 83개가 남았습니다. 여러 스레드가 DB에서 같은 수량(예: 100)을 동시에 읽고, 각자 1을 뺀 값(99)을 덮어쓰면서 서로의 갱신 내역을 날려버리는 **갱신 손실(Lost Update)** 이 발생한 것입니다.

### Phase 2. 비관적 락으로 정합성 확보

DB가 제공하는 락으로 갱신 손실을 막기로 했습니다. Spring Data JPA의 `@Lock(LockModeType.PESSIMISTIC_WRITE)`를 적용해 쿼리 레벨에서 `SELECT ... FOR UPDATE`가 걸리도록 했고, 한 스레드가 트랜잭션을 끝낼 때까지 다른 스레드의 접근을 막았습니다.

동일한 100명 동시 요청 테스트에서 남은 수량이 정확히 0으로 떨어져, 정합성은 확보했습니다.

### Phase 2-1. 비관적 락의 한계 (부하 테스트)

정합성은 잡았지만, 락 점유가 실제 트래픽에서 어떤 영향을 주는지 확인해야 했습니다. Docker + PostgreSQL 환경에서 JMeter로 부하를 걸었습니다.

- **시나리오:** 쿠폰 1만 장, 가상 사용자 3,000명이 1초 안에 동시 호출
- **정합성:** 3,000건 처리 후 정확히 7,000개 잔여 (유지됨)
- **처리량:** 약 28 TPS
- **평균 응답:** 4,429 ms (최대 13초)

하나의 트랜잭션이 row 락을 쥐고 있는 동안 나머지 수천 개 요청이 줄을 서서 기다리고, 결국 DB 커넥션 풀이 고갈되면서 응답이 4초 이상으로 치솟았습니다. 정합성은 지키지만 대규모 서비스에는 쓸 수 없는 구조임을 수치로 확인했습니다.

### Phase 3. Redis 분산 락 — 예상 밖의 결과

비관적 락의 디스크 I/O 부하를 줄이려고, 인메모리 기반 Redis 분산 락(Redisson)을 도입했습니다. 락 대기 시간은 10초로 두어 무한 대기를 막았습니다. 당연히 빨라질 거라 예상했지만, 결과는 오히려 더 나빠졌습니다.

| 지표 | 비관적 락 | Redis 분산 락 |
| --- | --- | --- |
| 처리량(TPS) | 27.9 /sec | **8.2 /sec** |
| 평균 응답 | 4,429 ms | **5,406 ms** |
| 에러율 | 0.00% | **34.00%** |

(쿠폰은 2,940장 발급, 나머지 60건은 10초 대기 초과로 정상 포기 — 방어 로직은 의도대로 작동)

**왜 더 느려졌나.** 원인은 Redis가 아니라 그 뒤에 있었습니다. Redis는 초당 수십만 건도 처리할 만큼 빠릅니다. 진짜 병목은 애플리케이션(Tomcat)과 DB(PostgreSQL) 사이의 자원 고갈이었습니다.

- **Tomcat 스레드 고갈 (에러율 34%의 원인):** 내장 Tomcat의 기본 최대 스레드는 200개입니다. 1초에 3,000개 요청이 몰리면 200개 스레드가 락을 잡거나 기다리며 묶이고, 나머지 요청은 스레드를 못 받아 큐에서 대기하다 Timeout으로 튕깁니다.
- **HikariCP 커넥션 풀 병목 (TPS 저하의 원인):** Redis에서 락을 아무리 빨리 얻어도, 실제 수량을 차감하려면 DB에 `UPDATE`를 날려야 합니다. 그런데 기본 커넥션 풀(HikariCP)은 10개뿐입니다. 락을 얻은 수백 개 스레드가 다시 10개의 커넥션을 두고 2차로 줄을 서는(Secondary Bottleneck) 셈이라, 자물쇠만 빨리 풀렸을 뿐 DB로 들어가는 문은 여전히 좁았습니다.

## 4. 구조 전환: 동기식 락 → 비동기 대기열

이 결과로 분명해진 건, "어떤 락을 쓰든 동기식으로 DB를 즉시 업데이트하는 구조 자체에 한계가 있다"는 점이었습니다. 그래서 락에 의존하는 방식을 버리고, 메시지 큐(Redis List)를 활용한 비동기 처리로 전면 개편했습니다.

1. **요청 단계 (빠르게):** 요청이 오면 DB를 거치지 않고 Redis 큐에 대기표만 발급한 뒤 곧바로 응답합니다. Tomcat 스레드 점유를 최소화하는 것이 핵심입니다.
2. **처리 단계 (비동기):** 별도 Worker 스레드가 큐에서 요청을 꺼내, DB 커넥션 풀이 감당할 수 있는 속도(예: 10개씩)로 Bulk Update를 수행합니다.

## 5. 인프라 튜닝: 마지막 병목

비동기 큐로 응답 속도와 TPS는 크게 올랐지만, 1초에 3,000명이 몰리자 이번엔 OS 레벨의 네트워크 소켓(TCP Backlog) 한계로 약 18%의 `Connection Refused`가 발생했습니다. 애플리케이션이 아니라 서버의 '출입구'가 막힌 것입니다. 마지막으로 인프라를 손봤습니다.

- **Tomcat 확장:** `application.yml`에서 `max-threads: 1000`, `accept-count: 10000`, `max-connections: 10000`으로 상향해 서버의 출입구와 대기열을 넓혔습니다.
- **클라이언트 병목 우회:** 초당 3,000 소켓을 여는 데서 JMeter 자체가 한계를 보여, HTTP 통신 구현체를 Java Native로 바꿔 우회했습니다.

### 최종 성능 비교

튜닝 후 1초 3,000명 동시 접속 테스트를 다시 돌린 결과입니다.



| 구분 | 비관적 락 (Phase 2) | 최종 (비동기 큐 + 인프라 튜닝) | 변화 |
| --- | --- | --- | --- |
| 평균 응답 | 4,429 ms | **267 ms** | 약 16.5배 단축 |
| 처리량(TPS) | 27.9 /sec | **1,550.4 /sec** | 약 55배 향상 |
| 에러율 | 0.00% | **0.00%** | 유실 없이 전량 처리 |

3,000건의 요청이 한 건의 유실도 없이 모두 큐에 적재됐고, Worker의 순차 처리를 거쳐 최종 재고가 10,000개에서 정확히 7,000개로 차감된 것을 확인했습니다.

## 6. 회고

이 프로젝트에서 배운 건, 백엔드 성능 최적화가 "코드를 잘 짜는 것"으로 끝나지 않는다는 점입니다. [코드 → DB 커넥션 → WAS 스레드 → OS 네트워크]로 이어지는 전 구간에서 가장 좁은 병목을 찾아 뚫는 과정에 가까웠습니다.

특히 인상 깊었던 건 Redis 분산 락을 넣었는데 오히려 느려진 순간이었습니다. "빠른 도구를 썼으니 빨라지겠지"라는 직관이 틀렸고, 병목은 늘 내가 보던 곳이 아니라 그 다음 레이어(커넥션 풀, 스레드 풀)에 숨어 있었습니다. 결국 28 → 8.2 → 1,550 TPS로 이어진 이 여정은, 성능을 감이 아니라 측정 지표로 판단하는 습관을 들이게 해준 경험이었습니다.
